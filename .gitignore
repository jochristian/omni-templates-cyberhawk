patches/install_cilium.yaml
patches/install_nfs-csi-driver.yaml
patches/install_argocd.yaml
charts
# Helm dependencies lock file
Chart.lock
# Claude Code superpowers specs (local design docs, not for repo)
**/docs/superpowers/
# Local audit reports
talos-audit-*.md

# Real paperless correspondent list is personal data and lives in
# ~/paperless-seed/. Defensive: never let a copy land in this public repo.
# (correspondents.example.yaml is placeholders and IS tracked.)
docs/paperless/correspondents.yaml
paperless-seed/
