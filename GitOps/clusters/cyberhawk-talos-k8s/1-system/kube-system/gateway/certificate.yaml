apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-cyberhawk-no
  namespace: kube-system
spec:
  secretName: wildcard-cyberhawk-no-tls
  dnsNames:
    - "*.cyberhawk.no"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
