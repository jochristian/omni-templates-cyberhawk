apiVersion: v1
kind: Service
metadata:
  name: librenms
  annotations:
    omni-kube-service-exposer.sidero.dev/port: "8000"
    omni-kube-service-exposer.sidero.dev/label: librenms
spec:
  type: LoadBalancer
  selector:
    app: librenms
    component: web
  ports:
    - name: http
      port: 80
      targetPort: 8000
