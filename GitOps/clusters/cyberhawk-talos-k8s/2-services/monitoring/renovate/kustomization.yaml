apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

generators:
  - 01-secret.yaml
  - 02-secret-forgejo.yaml

resources:
  - 10-configmap.yaml
  - 11-configmap-forgejo.yaml
  - 20-cronjob.yaml
  - 21-cronjob-forgejo.yaml

images:
  # Renovate self-hosted runner. Renovate updates THIS tag via PR too (see the
  # annotation, decoded by the customManager in the repo-root renovate.json).
  - name: renovate/renovate
    newTag: "43.288.0" # renovate: docker=renovate/renovate

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: renovate
